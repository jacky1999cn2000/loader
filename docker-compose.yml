version: '2'
services:
  test:
    build: .
    volumes:
      - .:/usr/src/app
      - ${HOME}/.aws:/root/.aws
    environment:
      S3_BUCKET: "youtube-farming"
      PROJECT_NAME: "test"
      NOTIFICATION_EMAIL: "liang.zhao.sfdc01@gmail.com"
      RUNNING_ENVIRONMENT: "local"
    working_dir: "/usr/src/app"
    entrypoint: "/usr/src/app/node_modules/nodemon/bin/nodemon.js -L --ignore node_modules/ --ignore public/ --ignore .tmp/ index.js"
